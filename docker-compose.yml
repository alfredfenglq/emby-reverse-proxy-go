services:
  emby-proxy:
    image: ghcr.io/gsy-allen/emby-proxy-go:v1.0
    container_name: emby-proxy
    ports:
      - "8080:8080"
    environment:
      - LISTEN_ADDR=:8080
    restart: unless-stopped
